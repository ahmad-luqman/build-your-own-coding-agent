pre-commit:
  commands:
    biome-check:
      glob: "*.{ts,tsx,js,jsx,json}"
      run: bunx biome check --staged --no-errors-on-unmatched
    typecheck:
      run: bunx tsc --noEmit

pre-push:
  commands:
    test:
      run: bun test
